package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Clean Utility / Minimal Theme Palette
val BrandGreenDark = Color(0xFF00210A)    // Extremely deep green for text/accents
val BrandGreenMedium = Color(0xFF006D32)  // Crisp, clean primary green
val BrandGreenLight = Color(0xFFA6F4B5)   // High-contrast vibrant mint green accent
val TealAccent = Color(0xFF006D32)        // Keep with crisp green tone

// Neutrals & Surfaces
val NeutralDark = Color(0xFF191C19)       // Soft graphite black
val LightBg = Color(0xFFF8FAF8)           // Fresh, ultra-soft green-tinted white
val CardBg = Color(0xFFFFFFFF)            // Plain white for high utility cards
val BorderColor = Color(0x2672796F)       // Soft border outlined contour (#72796F at 15% opacity)
val AccentYellow = Color(0xFF7D5700)      // Highly readable dark amber text

// Banner / State backgrounds
val LightGreenBg = Color(0xFFE8F5E9)      // Soft mint container
val LightYellowBg = Color(0xFFFFE082)     // Fresh honey-amber container
val LightBlueBg = Color(0xFFE3F2FD)       // Clear slate-blue container
val SoftRed = Color(0xFFBA1A1A)           // Refined status red
val LightRedBg = Color(0xFFFFDAD9)        // Soft red background container

